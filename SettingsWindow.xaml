<Window x:Class="GreenResourceMonitor.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreenResourceMonitor"
        mc:Ignorable="d"
        Title="Settings" Height="570" Width="600"
        Background="{StaticResource BackgroundDark}">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Electricity Price (EUR/kWh):" 
                   Foreground="{StaticResource TextLight}" VerticalAlignment="Center" 
                   Margin="0,0,10,15" Grid.Row="0" Grid.Column="0"/>
        <TextBox x:Name="PriceBox"
                 Grid.Row="0" Grid.Column="1"
                 Height="25" Margin="0,0,0,15"/>

        <TextBlock Text="CO₂ Intensity (g/kWh):" 
                   Foreground="{StaticResource TextLight}" 
                   VerticalAlignment="Center" Margin="0,0,10,15" 
                   Grid.Row="1" Grid.Column="0"/>
        <TextBox x:Name="CO2Box"
                 Grid.Row="1" Grid.Column="1" 
                 Height="25" Margin="0,0,0,15"/>

        <TextBlock Text="Sampling Interval (s):" 
                   Foreground="{StaticResource TextLight}" 
                   VerticalAlignment="Center" Margin="0,0,10,15" 
                   Grid.Row="2" Grid.Column="0"/>
        <TextBox x:Name="IntervalBox" 
                 Grid.Row="2" Grid.Column="1" 
                 Height="25" Margin="0,0,0,15"/>

        <TextBlock Text="Thermal Design Power (W):" 
           Foreground="{StaticResource TextLight}" 
           VerticalAlignment="Center" Margin="0,0,10,15" 
           Grid.Row="3" Grid.Column="0"/>
        <TextBox x:Name="TDPBox" 
         Grid.Row="3" Grid.Column="1" 
         Height="25" Margin="0,0,0,15"/>

        <TextBlock Text="Calibration Factor:" 
                   Foreground="{StaticResource TextLight}" 
                   VerticalAlignment="Center" Margin="0,0,10,15" 
                   Grid.Row="4" Grid.Column="0"/>
        <TextBox x:Name="CalibBox" 
                 Grid.Row="4" Grid.Column="1" 
                 Height="25" Margin="0,0,0,15"/>

        <TextBlock Text="Storage Mode:" 
                   Foreground="{StaticResource TextLight}" 
                   VerticalAlignment="Center" Margin="0,0,10,15" 
                   Grid.Row="5" Grid.Column="0"/>
        <ComboBox x:Name="StorageModeCombo" 
                  Grid.Row="5" Grid.Column="1" 
                  Height="25" Margin="0,0,0,15">
            <ComboBoxItem Content="CSV only" Tag="CSVOnly" />
            <ComboBoxItem Content="SQL Server only" Tag="SQLOnly" />
            <ComboBoxItem Content="Both" Tag="Both" />
        </ComboBox>

        <TextBlock Text="Load Region Data:" 
                   Foreground="{StaticResource TextLight}" 
                   VerticalAlignment="Center" Margin="0,0,10,15" 
                   Grid.Row="6" Grid.Column="0"/>
        <Button Content="Select Country..." 
                Click="CountryButton_Click" Style="{StaticResource SecondaryBtn}" 
                Grid.Row="6" Grid.Column="1" 
                HorizontalAlignment="Left"
                Width="150" Margin="0,0,0,15"/>

        <Border Grid.Row="7" Grid.ColumnSpan="2" Height="1" Background="#444" Margin="0,10,0,20"/>

        <TextBlock Text="Data Management:"
                   Foreground="{StaticResource TextLight}" 
                   FontWeight="Bold" Margin="0,0,0,10" 
                   Grid.Row="8" Grid.Column="0"/>
        <StackPanel Grid.Row="8" Grid.Column="1" Orientation="Vertical">
            <Button Content="Migrate CSV to Database" 
                    Click="MigrateCsvToDbButton_Click" 
                    Style="{StaticResource SecondaryBtn}"
                    Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
            <Button Content="Clear CSV Data" 
                    Click="DeleteCsvDataButton_Click"
                    Style="{StaticResource SecondaryBtn}" 
                    Background="{StaticResource DangerRed}"
                    Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
            <Button Content="Clear SQL Database" 
                    Click="DeleteSqlDataButton_Click" 
                    Style="{StaticResource SecondaryBtn}" Background="{StaticResource DangerRed}" 
                    Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
        </StackPanel>

        <StackPanel Grid.Row="10" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="Save Settings" Click="Save_Click" 
                    Style="{StaticResource ModernBtn}" Width="120" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="Cancel_Click" 
                    Style="{StaticResource SecondaryBtn}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>